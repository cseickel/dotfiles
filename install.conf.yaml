- clean:
    ~/:
      recursive: false
    ~/.config/nvim:
      recursive: true
- defaults:
    link:
        relink: true
        force: true
        relative: true
- create:
    - ~/.config/nvim/lua
    - ~/.config/nvim/UltiSnips
    - ~/.gnupg
- link:
    ~/start-arch-linux-docker.sh: start-arch-linux-docker.sh
    ~/start-code-server.sh: start-code-server.sh
    ~/.gitconfig: gitconfig
    ~/.gnupg/gpg-agent.conf: gpg-agent.conf
    ~/.tmux.conf: tmux.conf
    ~/.zshrc: zshrc
    ~/.config/nvim:
      glob: true
      path: config/nvim/*
      recursive: true
- shell:
  - [./tmux_install_plugins.sh, Installing tmux plugins.]
  - [nvim --headless -c "autocmd User PackerComplete quitall" -c "PackerSync", Installing nvim plugins.]

