[pull]
	ff = only
[user]
	email = cseickel@gmail.com
	name = Chris Seickel
	signingkey = /home/user/.ssh/chris-ssh-2025.pub

[pager]
    diff = delta
    show = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    features = side-by-side line-numbers decorations
    whitespace-error-style = 22 reverse

[delta "decorations"]
    commit-decoration-style = bold "#d7d700" box ul
    file-style = bold cyan
    file-decoration-style = bold cyan ul
    hunk-header-decoration-style = blue box
[commit]
	gpgsign = true
[tag]
	gpgSign = true
[credential "https://github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper = 
	helper = !/usr/bin/gh auth git-credential
[gpg]
	format = ssh
